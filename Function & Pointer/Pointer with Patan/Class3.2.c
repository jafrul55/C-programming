#include<stdio.h>
//Array with pointers:
int main()
{
  //Now we will do jog/Biyog
  //We will use here pointer 
  //we print value in int datatype.
   int a = 100;
   int *p = &a;
   printf("%d %d",p,(p+1));
   //Here we have been used int datatype
 //Here we use pointer and there are 4 bit different between two number
   return 0;
}

