#include<stdio.h>
//Array with pointers:
int main()
{
  //Now we will do jog/Biyog
  //Now we will use here long long:
   long long int a = 100;
   long long int *p = &a;
   printf("%d %d %d\n",*p,*(p+1),*(p+2));
 //Here we use pointer and there are 4 bit different between two number
   return 0;
}

